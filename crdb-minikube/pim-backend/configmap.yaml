## ConfigMap holding non-sensitive environment variables for PIM backend
apiVersion: v1
kind: ConfigMap
metadata:
  name: pim-backend-config
  namespace: pim
data:
  # CockroachDB connection string moved to a SealedSecret (DATABASE_URL).
  PORT: "3000"
  SKIP_DB_SEED: "false"
