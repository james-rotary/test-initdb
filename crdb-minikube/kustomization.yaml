namespace: crdb
resources:
  - namespace.yaml
  - services.yaml
  - cockroachdb-statefulset.yaml
  - bootstrap-sql-job.yaml
configMapGenerator:
  - name: bootstrap-sql
    files:
      - init.sql
generatorOptions:
  disableNameSuffixHash: true
